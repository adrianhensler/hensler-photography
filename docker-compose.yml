# Production Docker Compose
# Serves all 3 domains on standard ports (80/443)
# Caddy automatically manages TLS certificates for each domain

services:
  web:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./sites:/srv/sites:ro
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config

  # API service (uncomment when ready to deploy)
  # api:
  #   build: ./api
  #   restart: unless-stopped
  #   ports:
  #     - "4100:8000"
  #   volumes:
  #     - gallery-db:/data
  #     - gallery-images:/app/assets/gallery
  #   environment:
  #     - DATABASE_PATH=/data/gallery.db
  #     - JWT_SECRET_KEY=${JWT_SECRET_KEY}
  #     - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}

volumes:
  caddy-data:
  caddy-config:
  # Database and images volumes (uncomment when API is deployed)
  # gallery-db:
  # gallery-images:
