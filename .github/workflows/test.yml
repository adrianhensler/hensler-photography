name: Automated Testing

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Create external Caddy volumes
        # docker-compose.local.yml expects hensler_photography_caddy-data/-config to
        # already exist, since on the real dev host they're shared with the production
        # Caddy stack. A CI runner has no such volumes, so create empty ones here.
        run: |
          docker volume create hensler_photography_caddy-data
          docker volume create hensler_photography_caddy-config

      - name: Start test server
        run: |
          docker compose -f docker-compose.local.yml up -d
          # Wait for server to be ready
          sleep 5
          curl --retry 10 --retry-delay 3 --retry-connrefused http://localhost:8080/healthz

      - name: Run Playwright tests
        run: npm test

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots
          path: screenshots/
          retention-days: 30

      - name: Stop test server
        if: always()
        run: docker compose -f docker-compose.local.yml down

      - name: Check for test failures
        if: failure()
        run: |
          echo "❌ Tests failed! Please review the test results and screenshots."
          exit 1
