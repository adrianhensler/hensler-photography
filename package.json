{
  "name": "hensler-photography-testing",
  "version": "1.0.0",
  "description": "Visual testing for Hensler Photography sites",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "screenshot": "playwright test --grep @screenshot",
    "dev": "docker compose -f docker-compose.local.yml up -d && echo 'Test server running on http://localhost:8080'",
    "dev:stop": "docker compose -f docker-compose.local.yml down",
    "dev:logs": "docker compose -f docker-compose.local.yml logs -f web",
    "prod:start": "docker compose up -d",
    "prod:stop": "docker compose down",
    "prod:restart": "docker compose restart",
    "prod:logs": "docker compose logs -f web",
    "health": "curl -I http://localhost:8080/healthz && curl -I https://hensler.photography/healthz && curl -I https://liam.hensler.photography/healthz && curl -I https://adrian.hensler.photography/healthz",
    "backup": "sudo -E ./scripts/backup.sh",
    "backup:list": "restic -r /opt/backups/hensler_photography snapshots",
    "deploy": "echo 'Run: npm test && git push && docker compose restart'"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
